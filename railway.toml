[build]
dockerfilePath = "trafiker/Dockerfile.railway"
watchPatterns = ["trafiker/**"]

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
